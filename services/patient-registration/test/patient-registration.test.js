const { expect } = require('chai');

describe('Patient Registration Service', () => {
  describe('Patient Controller', () => {
    it('should create a placeholder test11111111', () => {
      expect(true).to.be.true;
    });
  });


});